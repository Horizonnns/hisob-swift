.DS_Store
# Проект генерируется из project.yml: `xcodegen generate`
*.xcodeproj
*.xcworkspace
xcuserdata/
DerivedData/
.build-dd/
.build/
.swiftpm/
*.hmap
*.ipa
*.dSYM.zip

# api
node_modules/
*.tsbuildinfo
.vercel
legacy-export.json
backup*.json
*-backup.json

# команда подписи — своя у каждой машины
Signing.xcconfig

# секреты: строки подключения и токен

# команда подписи — своя у каждой машины
Signing.xcconfig

# секреты: строки подключения и токен
# порядок важен — отрицание должно быть после всех правил
.env
.env.*
!.env.example
